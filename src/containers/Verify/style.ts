import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    buttonWrapper: {
        marginTop: 50,
        padding: 15
    },
    field: {
        padding: 15
    },
    notif: {
        padding: 15,
        textAlign: 'center',
        marginBottom: 15
    }
})
